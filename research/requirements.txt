# Research layer dependencies (Python >= 3.11)
#   python3 -m venv .venv && .venv/bin/pip install -r research/requirements.txt
duckdb>=1.0
pandas>=2.0
numpy>=1.26
pyarrow>=15.0
matplotlib>=3.8
jupyter
requests
